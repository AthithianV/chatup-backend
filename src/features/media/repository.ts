

export default class MediaRepository{

    uploadFile = async () => {
        try {
            
        } catch (error) {
            throw error;
        }
    }

    
    deleteFile = async () => {
        try {
            
        } catch (error) {
            throw error;
        }
    }
}